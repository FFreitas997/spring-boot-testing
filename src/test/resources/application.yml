spring:
  data:
    mongodb:
      username: francisco
      password: 1234567890
      host: localhost
      port: 27017
      database: studentdbtest
      authentication-database: admin
      uri: mongodb://francisco:1234567890@localhost/studentdbtest?ssl=false&authSource=admin

# H2 database configuration
#
#spring:
#  datasource:
#    url: jdbc:h2:mem:mydb
#    url: jdbc:h2:file:/data/demo // file based storage to persist data even after application restart
#    url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;AUTO_RECONNECT=TRUE;MODE=PostgreSQL; // Full H2 Connection URL
#    username: sa
#    password: password
#    driverClassName: org.h2.Driver
#  jpa:
#    database-platform: org.hibernate.dialect.H2Dialect
#  h2:
#    console.enabled: true // Enable H2 console with URL: http://localhost:8080/h2-console